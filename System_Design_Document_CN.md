
# 自托管企业级 RAG 后端系统
# 系统设计文档

---

# 1. 执行摘要

本文档描述了一个自托管的、企业级 Retrieval-Augmented Generation（RAG）后端平台的系统设计。本系统集成了 Java Spring Boot 后端 API、基于 Python FastAPI 的 RAG 微服务，以及由 Ollama 提供支持的本地大语言模型和向量嵌入模型。

系统的主要设计目标包括：

- 完全本地运行，无需任何云服务依赖
- 通过安全的 API Key 认证实现多租户隔离
- 异步文档摄取（ingestion）处理管道
- 元数据和向量嵌入的持久化存储
- 以后端为权威的数据生命周期管理
- 模块化、生产级架构，并具备良好的可观测性

系统支持用户上传 PDF 文档，将文档摄取到向量数据库中，并基于检索结果生成带引用来源的可靠回答。

---

# 2. 高层架构

## 核心组件

| 组件 | 技术 | 职责 |
|---------|------------|----------------|
| 后端 API | Spring Boot | 身份认证、任务管理、文档元数据管理 |
| RAG 服务 | FastAPI | 文档摄取、分块处理、向量嵌入、检索 |
| 向量数据库 | ChromaDB | 持久化向量存储 |
| LLM / 向量模型 | Ollama | 向量生成和答案生成 |
| 数据库 | H2 | 持久化任务和文档元数据 |
| 存储系统 | 本地文件系统 | 按租户隔离的持久化存储 |

## 请求逻辑流程

客户端  
→ 后端 API（Spring Boot）  
→ 摄取 Worker  
→ RAG 服务（FastAPI）  
→ Ollama（大语言模型和向量模型）  
→ ChromaDB  

该架构将控制平面逻辑（backend API）与计算平面逻辑（RAG service）分离，从而保证系统具有良好的可扩展性和可维护性。

---

# 3. 核心设计原则

## 3.1 后端权威架构设计

文档元数据仅存储和管理在后端数据库中，数据库作为系统唯一的权威数据源。

后端负责：

- 文档生命周期管理
- 任务调度与状态管理
- 租户隔离控制

RAG 服务仅作为计算引擎运行，不具备文档管理权威。

---

## 3.2 多租户隔离

系统通过以下机制实现租户隔离：

- API Key 身份认证
- Tenant ID 请求头
- 按租户划分的存储目录
- 按租户划分的向量命名空间和注册表

该设计确保不同租户之间的数据完全隔离。

---

## 3.3 异步摄取处理管道

文档摄取流程如下：

上传 → 创建任务 → Worker 处理 → 向量存储 → 元数据持久化

该设计带来的优势包括：

- API 不会被长时间阻塞
- 系统具有更好的扩展能力
- 支持故障隔离与恢复

---

## 3.4 软删除模型

文档删除采用软删除策略：

- 在后端数据库中将文档标记为已删除
- 在 RAG 服务中删除对应向量数据（尽最大努力执行）
- 禁止访问已删除文档

该设计支持系统审计能力和安全的数据生命周期管理。

---

# 4. 数据模型设计

## 文档实体（Document Entity）

字段包括：

- docId
- tenantId
- filename
- status
- createdAt
- deletedAt
- lastIngestTaskId

## 任务实体（Task Entity）

字段包括：

- taskId
- tenantId
- status
- createdAt
- updatedAt
- docId

---

# 5. 请求流程

## 文档摄取流程

客户端 → 后端 API  
后端 → 创建摄取任务  
Worker → 将文档发送至 RAG 服务  
RAG 服务 → 执行分块、向量嵌入并存储向量  
Worker → 更新后端数据库元数据  

---

## 查询流程

客户端 → 后端 API  
后端 → 将查询请求转发至 RAG 服务  
RAG 服务 → 检索相关内容并生成带引用的答案  
后端 → 返回答案和引用信息给客户端  

---

# 6. 存储设计

持久化存储位置包括：

后端数据库：

storage/h2/

向量存储：

storage/chroma/

按租户划分的存储：

storage/tenants/<tenantId>/

---

# 7. 故障处理

系统可以处理以下故障类型：

- 身份认证失败
- 文件缺失或无效
- 非法或错误请求
- 内部处理错误

所有错误均记录日志，便于调试和系统监控。

---

# 8. 安全设计

系统安全机制包括：

- API Key 身份认证
- 严格的租户隔离机制
- 后端控制的文档生命周期管理

这些机制确保系统资源访问的安全性和隔离性。

---

# 9. 可扩展性设计

系统架构支持未来扩展，包括：

- 将 H2 替换为 PostgreSQL 或其他生产级数据库
- 水平扩展后端 API 实例
- 独立扩展 RAG 服务
- 支持分布式向量存储系统

---

# 10. 设计权衡

关键设计决策：以后端为权威的数据管理架构

优势：

- 强一致性保证
- 清晰的数据生命周期管理
- 简化系统协调逻辑

权衡：

- 增加后端的职责和协调复杂度

---

# 11. 部署模型

系统通过 Docker Compose 进行部署。

部署容器包括：

- backend-api
- rag-service
- ollama

该方式支持可重复、隔离的部署环境。

---

# 12. 可观测性

系统通过容器日志实现可观测性：

docker logs backend-api  
docker logs rag-service  

支持系统调试和运行监控。

---

# 13. 总结

本系统实现了一个生产级、自托管的 RAG 后端平台，具备以下特性：

- 多租户隔离
- 异步文档摄取管道
- 持久化存储架构
- 后端权威生命周期管理
- 模块化微服务架构

本系统适用于：

- 企业内部部署
- 后端工程作品集展示
- 系统设计能力展示
- AI 后端基础设施学习与实验

